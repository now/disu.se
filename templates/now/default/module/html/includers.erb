<dt>Included by</dt>
<dd>
  <dl class="modules includers">
  <% @includers.each do |name, includer| %>
    <dt><%= linkify(includer, name) %></dt>
    <dd><%= htmlify(includer.docstring.summary) %></dd>
  <% end %>
  </dl>
</dd>
