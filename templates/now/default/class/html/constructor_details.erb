<section id="constructor">
  <h1>Constructor</h1>
  <% if @constructor.namespace != object %>
    <p class="notice">This class inherits its constructor from <%= linkify(@constructor, object.relative_path(@constructor.namespace.path)) %>.</p>
  <% else %>
    <%= yieldall :object => @constructor %>
  <% end %>
</section>
