<dt>Subclasses</dt>
<dd>
  <dl class="subclasses">
  <% @subclasses.each do |name, subclass| %>
    <dt><%= linkify(subclass, name) %></dt>
    <dd><%= htmlify(subclass.docstring.summary) %></dd>
  <% end %>
  </dl>
</dd>
