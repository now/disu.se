<dt>Subclasses</dt>
<dd>
  <dl class="children">
  <% @subclasses.each do |name, child| %>
    <dt><%= linkify(child, name) %></dt>
    <dd><%= htmlify(child.docstring.summary) %></dd>
  <% end %>
  </dl>
</dd>
