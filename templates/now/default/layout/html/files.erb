<% if @files && @files.size > 0 %>
  <h2>File Listing</h2>
  <ul id="files">
  <% n = 1 %>
  <% @files.each_with_index do |file, i| %>
    <li class="r<%= n %>"><p><%= link_file(file) %></p></li>
    <% n = n == 2 ? 1 : 2 %>
  <% end %>
  </ul>
<% end %>
