<dt>See Also</dt>
<dd>
<% if object.tags(:see).size == 1 %>
  <p><%= linkify(object.tag(:see).name, object.tag(:see).text) %></p>
<% else %>
  <ul class="see">
  <% for tag in object.tags(:see) %>
    <li><p><%= linkify(tag.name, tag.text) %></p></li>
  <% end %>
  </ul>
<% end %>
</dd>
